# Workspace Rules

## Protected Files
- **Never edit `todo.txt`** (e.g. `docs/todo.txt` or any `todo.txt` files). The user edits this file manually while tasks are running; do not modify, delete, or overwrite it under any circumstances.
